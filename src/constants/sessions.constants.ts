export enum PlatformType {
  ANDROID = 'android',
  IOS = 'ios',
  WEB = 'web',
}

export enum DeviceType {
  PHONE = 'phone',
  TABLET = 'tablet',
  DESKTOP = 'desktop',
  LAPTOP = 'laptop',
  OTHER = 'others',
}

export enum DeviceOsType {
  WINDOWS = 'windows',
  MACOS = 'macos',
  LINUX = 'linux',
  ANDROID = 'android',
  IOS = 'ios',
  OTHER = 'others',
}

export enum DeviceModelType {
  IPHONE = 'iphone',
  SAMSUNG = 'samsung',
  GOOGLE = 'google',
  ONEPLUS = 'oneplus',
  XIAOMI = 'xiaomi',
  OTHER = 'others',
}
